/**
 * Generates unique temporary file paths for old and new files.
 *
 * @returns Object containing paths for temporary old and new files.
 */
export declare const getTempFilePaths: () => {
    oldFilePath: string;
    newFilePath: string;
};
